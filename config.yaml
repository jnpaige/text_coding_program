# text_coding_program configuration

# PDF source — directory of PDFs or pdf_ocr output root (<trinomial>/ subdirs)
pdf_dir: 'C:\Users\jpaige\Desktop\local_repositories\2024_forms_chunk_3_surya_docling_5_15_2026\2024_forms_chunk_3_surya_docling_5_15_2026'

# Codebook trait JSON files — output of codebook_tools
codebook_dir: 'C:\Users\jpaige\Desktop\research_repositories\codebook_tools\test_output\Site_Form_v1.1'

# Segment map (optional) — segmenter run output dir with <trinomial>.segments.json
# When set, the viewer filters pages to the relevant segment's pages.
# Leave empty or omit to show all pages.
segments_dir: ''

trinomial_pattern: '(\d{2}[A-Z]{2}\d+)'

output_dir: 'runs'

# DPI for rendering PDF pages as images in the browser
page_dpi: 150

# Server settings
host: '127.0.0.1'
port: 8090
