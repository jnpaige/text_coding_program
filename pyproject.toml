[project]
name = "text-coding-program"
version = "0.1.0"
description = "Browser-based interactive text coding against structured codebooks, with PDF page viewer and segment-aware navigation"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "pymupdf>=1.24",
    "pyyaml>=6.0",
    "openpyxl>=3.1",
]
